

function renderPersoenlich(){
  const e=activeCharacter();
  if(!e)return'<div class="section-header"><div><h2>Persönliche Akte</h2><div class="sub">Dein Charakterprofil</div></div></div>\n      <div class="card"><div class="empty-state">Kein aktiver Charakter. Erstelle oder wähle einen in der Charakterverwaltung.</div></div>';
  const t=charName(e).trim().toLowerCase(),n=DB.akten.filter(e=>e.name.trim().toLowerCase()===t),a=DB.waffenschein.filter(e=>e.name.trim().toLowerCase()===t),i=DB.fuehrerschein.filter(e=>e.name.trim().toLowerCase()===t);
  return`\n    <div class="section-header"><div><h2>Persönliche Akte</h2><div class="sub">Dein Charakterprofil &amp; öffentliche Einträge</div></div></div>\n    <div class="row-2">\n      <div class="card">\n        <div class="char-main" style="margin-top:0;">${avatarHTML(e,"avatar-lg")}<div><div class="c-name">${esc(charName(e))}</div><div class="c-role">${esc(e.fraktion)}</div></div></div>\n        <div class="stat-grid">\n          <div class="stat-box"><div class="v">${e.alter||"–"}</div><div class="l">Alter</div></div>\n          <div class="stat-box" style="grid-column:span 2;"><div class="v" style="font-size:13px;">${e.wohnort?esc(e.wohnort):"–"}</div><div class="l">Wohnort</div></div>\n        </div>\n        <div style="font-size:12px; color:var(--muted); margin:-6px 0 16px;">${e.geburtsdatum?"Geboren am "+fmtDateShort(e.geburtsdatum):""}</div>\n        ${"Bürger"!==e.fraktion?`\n          <div style="padding:14px; border-radius:12px; background:var(--bg); display:flex; align-items:center; justify-content:space-between; gap:10px; flex-wrap:wrap;">\n            <div><div style="font-weight:700; font-size:13.5px;">${e.onDuty?"Aktuell im Dienst":"Nicht im Dienst"}</div><div style="font-size:11.5px; color:var(--muted);">Dienststatus · ${esc(e.fraktion)}</div></div>\n            <button class="btn ${e.onDuty?"btn-danger":"btn-primary"} btn-sm" onclick="toggleOwnDuty()">${e.onDuty?"Dienst beenden":"Dienst antreten"}</button>\n          </div>`:'<div class="empty-state small">Als Bürger gibt es keinen Dienststatus.</div>'}\n      </div>\n      <div class="card">\n        <h3 style="font-size:15px; font-weight:700; margin-bottom:14px;">Öffentliche Einträge zu deinem Charakter</h3>\n        <div style="font-size:12px; font-weight:700; color:var(--muted); text-transform:uppercase; letter-spacing:.3px; margin-bottom:8px;">Personenakte</div>\n        ${n.length?n.map(e=>`<div class="vermerk"><div class="vermerk-head"><b>${e.vermerke.length} Vermerk(e)</b></div><p>${esc(e.info||"Keine weiteren Angaben.")}</p></div>`).join(""):'<div class="empty-state small">Keine Akte hinterlegt.</div>'}\n        <div style="font-size:12px; font-weight:700; color:var(--muted); text-transform:uppercase; letter-spacing:.3px; margin:16px 0 8px;">Waffenscheine</div>\n        ${a.length?a.map(e=>`<div class="vermerk"><div class="vermerk-head"><b>${esc(e.typ)}</b><span>${fmtDateShort(e.datum)}</span></div><p>Ausgestellt von ${esc(e.ausgestelltVon)}</p></div>`).join(""):'<div class="empty-state small">Kein Waffenschein hinterlegt.</div>'}\n        <div style="font-size:12px; font-weight:700; color:var(--muted); text-transform:uppercase; letter-spacing:.3px; margin:16px 0 8px;">Führerscheine</div>\n        ${i.length?i.map(e=>`<div class="vermerk"><div class="vermerk-head"><b>Klasse ${esc(e.klasse)}</b><span>${fmtDateShort(e.datum)}</span></div><p>Ausgestellt von ${esc(e.ausgestelltVon)}</p></div>`).join(""):'<div class="empty-state small">Kein Führerschein hinterlegt.</div>'}\n      </div>\n    </div>`
}
